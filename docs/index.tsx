import * as React from 'react';
import { Box, BoxProps, Text, TextProps } from '../src';

export const BoxForDoc = (props: BoxProps) => <Box {...props} />;
export const TextForDoc = (props: TextProps) => <Text {...props} />;
